"""Audit package -- append-only audit-log writers.

Hosts spec 019's network_rate_limit_audit module (per-(IP, minute)
rejection coalescer + background flush task) and may host other
infrastructure-tier audit writers in future specs.
"""

# === Secrets & credentials (highest priority) ===
.env
*.pem
*.key
*.crt
*.p12
*.fernet

# Pre-commit 2MS staging dir (created+deleted per scan; never committed)
.2ms-scan-*/

# === Claude Code ===
.claude/settings.json
.claude/settings.local.json
.claude/credentials*
.claude/auth*
.claude/*.key
.claude/*.token
.claude/*.lock
CLAUDE.md
CLAUDE.local.md
.claude/CLAUDE.md

# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.python-version
.ruff_cache/
.mypy_cache/
*.egg-info/

# === Node (Phase 2 web UI) ===
node_modules/

# === Database ===
*.db
*.db-shm
*.db-wal
data/
pgdata/
postgres-data/

# === Docker ===
docker-compose.override.yml

# === Logs ===
*.log
logs/

# === Local test plans (not for GitHub) ===
test-plan-*.md

# === Local working files (gitignored by *.local.md convention) ===
# Operator-internal notes, action plans, and trackers stay local-only.
*.local.md

# === Local-only working zone (private notes, scratch) ===
# Files under this path stay on disk for the operator's reference but
# never enter the public tree. Move things here instead of deleting if
# you want to keep them locally.
local/

# === Local exploratory transcripts (working notes; redact before publishing) ===
docs/sacp-shakedown-threat-model-session.md

# === Internal ops manual (deploy-operator audience; not for public repo) ===
docs/operational-runbook.md

# === OS ===
.DS_Store
Thumbs.db

# === IDE ===
.vscode/
.idea/
*.swp
*.swo
*~
.claude/worktrees/

# === Coverage and test artifacts ===
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.tox/

# === Build artifacts ===
build/
dist/
*.whl

# === Backup files ===
*.bak
*.orig
*.rej
